// require the chalk package and use it to color and style text in the terminal.

// Write a simple script that uses chalk to print a colorful message.

// Run app.js using Node.js and observe the colorful output in the terminal.

import chalk from "chalk";

console.log(chalk.blue("This is the first string of output"));
console.log(chalk.yellow("This is the second string of output"));